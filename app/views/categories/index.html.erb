<h1>MENU</h1>

<% @categories.each do |c|%>
<%= link_to c.name, category_products_path(c) %> <br>
<% end %>
<br>
<% if logged_in? && current_user.admin? %>
    <%= link_to 'Add Category', new_category_path %>
    <%# <%= link_to 'Delete product', new_category_product_path(@category) %> 
<% end %>